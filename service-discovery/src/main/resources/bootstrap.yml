server:
  port: 8761

spring:
  application:
    name: "discovery-server"
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false